package net.vibmc.world.block;

/** The few block properties vib-MC actually uses, encoded in the 1.12 data nibble. */
public enum StateValue {
    LEVEL,
    FACING,
    HALF,
    AXIS,
    ROTATION
}